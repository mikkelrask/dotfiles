#!/usr/bin/env bash

STYLE="$HOME/.config/hypr/wofi/src/mocha/style.css"

if [[ ! $(pidof rofi) ]]; then
    rofi -show drun --style "${STYLE}"  --insensitive --case-sensitive --hide-scroll --show-icons --allow-images
else
    pkill rofi
fi
